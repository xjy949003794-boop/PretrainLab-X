seed: 1337
tokenizer_name: local_byte_level_bpe
data_dir: data/tokenized
output_dir: .
max_seq_len: 256
batch_size: 8
gradient_accumulation_steps: 8
max_steps: 500
warmup_steps: 25
learning_rate: 0.0003
min_learning_rate: 0.00003
weight_decay: 0.1
grad_clip: 1.0
precision: bf16
eval_interval: 50
eval_batches: 10
save_interval: 100
log_interval: 10
dim: 512
n_layers: 10
n_heads: 16
n_kv_heads: 4
multiple_of: 256
dropout: 0.0
rope_base: 10000.0
log_name: day3_resume_metrics.jsonl
checkpoint_prefix: day3_resume
summary_name: day3_resume_summary.json
wandb_enabled: true
wandb_project: PretrainLab-X
wandb_run_name: day3_resume_from_step400
